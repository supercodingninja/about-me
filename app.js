// As a developer, I want to provide five questions to the user, so that they can guess the answers and get to know me.
//
// As a developer, I want to notify the user after each question, so they are aware whether they were correct or not in their answers. -->
//
// As a developer, I want to be able to debug my code by using well structured console messages.
//
// As a developer, I want to manage my code through an online version control system, so I have the ability to work with others, manage my revisions, and keep a thorough record of my progress.
'use strict'

var userName = prompt ('What is your' ' name?' + name);
  return ('Very nice to meet you, ' + name '.'+' I always need new friends.');

computerPrompt = prompt ('Do you know I can tell a lot about a person, when I meet them?  I can; and I will prove it to you!  I will ask you 5 yes or no questions; and I will guess them right: do you want to see if I am right?!')

  var userChoice = prompt();
// What if a user makes an inappropriate choice like 'Get out of my face!'? How can I extend the function to handle that? //
  if (userChoice == 'YES', 'Yes', 'yes', 'Y', 'y') {
          // Show 'Outcome' //
          return ('Yes, you want to play; GREAT!');

      else if (userChoice == 'NO', 'No', 'no', 'N', 'n') {
          // Show 'Outcome' //
          return ('No, you do not want to play...I guess that about you, too!');
      }

      else (userChoice !== 'YES', 'Yes', 'yes', 'NO', 'No', 'no') {
          // Show 'Outcome' //
          return ('Like I said, I know a lot about you, already.');
      }
    }


var userChoice = userPersonailty;
var userPersonailty = prompt();

  computerPrompt('Do you believe in equality?')
    if (userPersonailty == 'YES', 'Yes', 'yes', 'Y', 'y') {
            // Show 'Outcome' //
            return ('GREAT!');

        else if (userPersonailty == 'NO', 'No', 'no', 'N', 'n') {
            // Show 'Outcome' //
            return ('Call the police!  Rutabagas and squashs have rights!');
              do {
                if (userPersonailty == 'YES', 'Yes', 'yes', 'Y', 'y') {
                      // Show 'Outcome' //
                      return ('GREAT!');

                  else if (userPersonailty == 'NO', 'No', 'no', 'N', 'n') {
                      // Show 'Outcome' //
                      return ('I knew you were a troubled person.')
              } while (true);

              return ('GREAT!');

              else (userPersonailty !== 'YES', 'Yes', 'yes', 'NO', 'No', 'no') {
                  // Show 'Outcome' //
                  return ('Like I said, I really know a lot about you, already.');
              }
        }
      }

      computerPrompt('Do you believe people are nice when they share?')
      if (userPersonailty == 'YES', 'Yes', 'yes', 'Y', 'y') {
              // Show 'Outcome' //
              return ('I think you are correct.');

          else if (userPersonailty == 'NO', 'No', 'no', 'N', 'n') {
              // Show 'Outcome' //
              return ('Stop watching too, many movies.');
          }

          else (userPersonailty !== 'YES', 'Yes', 'yes', 'NO', 'No', 'no') {
              // Show 'Outcome' //
              return ('And the world, will be a better place...');
          }
        }

        computerPrompt('Do you believe that we just need to show love one to another?')
        if (userPersonailty == 'YES', 'Yes', 'yes', 'Y', 'y') {
                // Show 'Outcome' //
                return ('You are terrific!');

            else if (userPersonailty == 'NO', 'No', 'no', 'N', 'n') {
                // Show 'Outcome' //
                return ('You know, I have a teddy bear...');
            }

            else (userPersonailty !== 'YES', 'Yes', 'yes', 'NO', 'No', 'no') {
                // Show 'Outcome' //
                return ('...for you, and me- just wait, and see.');
            }
          }

          computerPrompt('Do you like wings?')
          if (userPersonailty == 'YES', 'Yes', 'yes', 'Y', 'y') {
                  // Show 'Outcome' //
                  return ('Really? Me, too!');

              else if (userPersonailty == 'NO', 'No', 'no', 'N', 'n') {
                  // Show 'Outcome' //
                  return ('No, problem.');
              }

              else (userPersonailty !== 'YES', 'Yes', 'yes', 'NO', 'No', 'no') {
                  // Show 'Outcome' //
                  return ('Think of your fellow man, lend him a helping hand, Put a little love in your heart, You see, it is getting late, oh, please do not hesitate, Put a little love in your heart, And the world will be a better place, And the world will be a better place for you and me, You just wait and see, Another day goes by, and still the children cry, Put a little love in your heart, If you want the world to know, we will not let hatred grow, Put a little love in your heart, And the world will be a better place, And the world will be a better place for you and me, You just wait and see. Wait and see, Take a good look around and if you are looking down, Put a little love in your heart, I hope when you decide kindness will be your guide, Put a little love in your heart, And the world will be a better place, And the world will be a better place for you and me, You just wait and see, Put a little love in your heart, Put a little love in your heart!');
              }
            }

            computerPrompt('So, where are we going for lunch- I knew you like me- you are buying me lunch, right?')
            if (userPersonailty == 'YES', 'Yes', 'yes', 'Y', 'y') {
                    // Show 'Outcome' //
                    return ('Yes, I knew I like you!');

                else if (userPersonailty == 'NO', 'No', 'no', 'N', 'n') {
                    // Show 'Outcome' //
                    return ('I eat cheap...');
                }

                else (userPersonailty !== 'YES', 'Yes', 'yes', 'NO', 'No', 'no') {
                    // Show 'Outcome' //
                    return ('Think of your fellow man...');
                }
              }

function compare (choice1, choice2) {

  var userPersonailty = choice1;
  var computerChoice = choice2;

  if (choice1 > choice2) {
      // Show 'Outcome' //
      return ('You Win!  There is more to you than I thought!');
    }

  else (choice1 < choice2) {
     Show 'Outcome'
     return ('See, I always know a character!');
    }
}
